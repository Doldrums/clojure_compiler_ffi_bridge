# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: ClojureCompilerFfiBridgeBindings
description: |
  Bindings for `src/clojure_compiler_ffi_bridge.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/clojure_compiler_ffi_bridge_bindings_generated.dart'
headers:
  entry-points:
    - 'src/clojure_compiler_ffi_bridge.h'
  include-directives:
    - 'src/clojure_compiler_ffi_bridge.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
